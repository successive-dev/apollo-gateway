type Query {
    getUsers: [User]
    getPosts: [Post]
}

type Mutation {
    addUser(data: IData): User
    addPost(text: String): Post
    updatePost(id: String, text: String): Post
}

type Subscription {
    newPost: Post 
}